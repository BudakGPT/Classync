## Classync — Demo Script & Decisions Log
\nAppend-only. One entry per decision made during Hack Day.

| Time | Who | Decision | Reason |
|---|---|---|---|
| 18 Sep 09:00 | Helven | Removed Fastify — using Next.js route handlers instead | One fewer process at 3 AM |
| 18 Sep 09:00 | All | LLM label merging → deterministic normalization + select | Invisible in demo, removes failure point |
| 18 Sep 09:00 | All | Cut: quiz confirm, /split, /reanswer, class-section roles, audit log, /help | 24h from scratch; none affects core loop |
| 18 Sep 2026 | Erik | `/tasks` uses a task selector before per-item actions | Discord permits at most five component rows; ten full item action groups cannot fit in one ephemeral message. |
| 18 Sep 14:15 | Erik | Private Concept Rooms with voluntary join, per-assignment categories, debounced TA DMs, and due-date-gated closure | Replaced forum threads with private text channels under assignment categories; allows direct join without requiring Stuck status, keeps rooms open after answer, and gates closure by task deadline. |
| 18 Sep 12:50 | Helven | `seed.ts` moved out of core's build tsconfig into `tsconfig.seed.json`, chained into `npm run typecheck` | Fixes TS6059 (rootDir). Alternative was dropping `prisma/**/*` from typecheck entirely — rejected: seed.ts is 209 lines and `m13` reseeds from it for the video at 05:00, so silent breakage there is unacceptable |
| 18 Sep 12:50 | Helven | Bot error reply object marked `as const` | `flags: MessageFlags.Ephemeral` widened to the enum and failed `BitFieldResolvable`. Alternative was annotating as `InteractionReplyOptions` — `as const` is smaller and needs no extra import |
| 18 Sep 12:50 | Helven | Auto-ingest (B6) gated behind `ENABLE_AUTO_INGEST`, default false | It fires on every `messageCreate` and is the only feature that can fail on stage; `/ta add-item` is the scripted fallback. Alternative was commenting the listener out — rejected: a flag re-enables it at 18:00 with zero code change, and missing/unset env fails safe to disabled |

| 18 Sep 14:40 | Helven (agent) | Schema: add `MatchState` enum, `Match` model, `Student.helperOptIn`, back-relations on Student/Item/Concept (peer-matching handoff §2). No message table. | Relayed text is never stored; privacy invariant, not a shortcut |
| 18 Sep 14:40 | Helven (agent) | Peer help runs as a bot-relayed DM conversation, not a private Discord thread | A thread exposes both members' identities the moment they are added; relay keeps the receiver anonymous until they choose to reveal |
| 18 Sep 14:40 | Helven (agent) | `/tasks` status buttons, Stuck picker, Request TA help and Privacy confirm moved from an in-command collector to a component router (`apps/bot/src/interactions/router.ts`, prefix before first `:`) | This branch had no button flow; the peer-match trigger and DM buttons need a persistent router, and the collector-based consent could not handle DM taps |
| 18 Sep 14:40 | Helven (agent) | `acceptMatch`/`resolveMatch` return only the `Match`; the bot resolves the other side via `getActiveMatchForUser` | Keeps `getActiveMatchForUser` the single path that returns both Discord IDs (handoff §3 privacy check) |
| 18 Sep 15:05 | Helven (agent) | `packages/core/src/index.ts` re-exports every service by name instead of `export *`; core stays CommonJS (no `"type": "module"`) | Since 5142804 the ESM bot could not start at all (`does not provide an export named getItemsByGuild`): Node's CJS lexer only sees esbuild's named-export annotation. Restoring `"type": "module"` would re-break the Vercel build |
| 18 Sep 2026 | Helven (agent) | `chores/merging` integrates `dev`, `feat/dashboard`, `chores/typecheck-gate-and-autoingest-flag`, `feat/peer-matching` as one merge commit each, in that order | Keeps every teammate's commits and authorship; GitHub marks the source PRs merged. `feat/docs` is already in main and `feat/classync-bot` is byte-identical to `dev`, so both were skipped |
| 18 Sep 2026 | Helven | `dev` is the base for the bot; peer matching is ported onto it | `dev` and `feat/peer-matching` both rewrote `/tasks`, `ui/tasks.ts` and the entrypoint from the same parent. Chosen over the reverse port because rooms + `/setup` are the larger surface |
| 18 Sep 2026 | Helven | Auto-ingest (B6) deleted, `MessageContent` intent dropped, `ENABLE_AUTO_INGEST` flag removed | `dev` removed it to clear the disallowed-intents error; `chores` had only flag-gated it. DM relay still reads DM content, which Discord exempts from the privileged intent |
| 18 Sep 2026 | Helven | Stuck flow: save status → peer offer (DM); concept rooms stay behind their own **Ask / Join Room** button | Chosen over "peer match first, room as fallback" to keep Erik's room flow untouched and the port small |
| 18 Sep 2026 | Helven | Schema is the union: `TopicRoom`/`TopicRoomMember` (dev) + `Match`/`helperOptIn` (peer-matching). Rooms accepted as in scope for PRD v2 | Integrator sign-off given in session. `db:push` not run locally: UNVERIFIED |
| 18 Sep 2026 | Helven | `chores/finalize`: the mock client dashboard at `/` (views/, store/, data/, hash router, 13 pages) is deleted; the five PRD pages under `/g/[guildId]` are the only web app, restyled with the existing tokens and `components/ui` primitives | The mock had no auth and its `getDbSyncState` action fed per-student statuses, sub-floor counts and the student list to any caller (privacy invariants 1 and 2). Keeping its shell as chrome was rejected: the shell read from the mock store and would have needed a rewrite anyway |
| 18 Sep 2026 | Helven | Web mutations take the author from the Auth.js session and pass a TA gate (`lib/session.ts`); `/api/answer-status` is TA-gated through the answer's guild | `submitAnswer` accepted `authorUserId` from the client and the status route was public |
| 18 Sep 2026 | Helven | Concept rooms are read-only on the web (state badge, member count, Discord jump link); moderation stays in `/ta close-room` / `reopen-room` / `close-task-rooms`. Placeholder `actions/rooms.ts` deleted | The web may not perform Discord side effects, so a browser "Close room" could only flip the DB row while the channel stayed writable. A room outbox (pending-action column + bot job) was rejected for Hack Day |
| 18 Sep 2026 | Helven | `/tasks` task detail regains **In progress** and **Done** (first row) and reminder DMs regain **Done** | PRD B2/B5 and demo step 2; without them W4's aggregate and the reminder "not DONE" rule had no inputs |
| 18 Sep 2026 | Helven | Fifth reporter sees "**5 students flagged this**" (honest total), not "5 others" | DEMO_SCRIPT §5; matches the privacy floor of five and Malik's narration |
| 18 Sep 2026 | Helven | Schema: add `HelpRequest.displayName String?`, written by the bot from the guild display name when the student confirms **Request TA help**; drop unused `Guild.helpForumChannelId`, `Concept.aliases`, `Item.sourceMessageId` | W3 needs requester names and the web cannot call Discord. The request row is already the only identity path and is deleted on revocation, so the name has the same lifetime. The three columns belonged to the superseded forum design and the deleted auto-ingest |
| 18 Sep 2026 | Helven | Removed `packages/core/src/llm.ts`, `parseAnnouncement`, `@anthropic-ai/sdk`, `ANTHROPIC_API_KEY`; removed `@dicebear/*` (mock avatars) from root and web | Auto-ingest was deleted on `dev`, so nothing called the LLM; dicebear only served the mock. `lucide-react` and the Plus Jakarta font stay as the dashboard's icon and type set (no shadcn install, no new radix packages) |
| 18 Sep 2026 | Helven | Erik's `/setup form` / `auto` / `panel` academic provisioner stays; the hardcoded `http://localhost:3000` dashboard link becomes `WEB_URL` (declared in `.env.example`, default localhost) | Low risk to the bot lane; the panel embed would have shown a dead link on stage |
| 18 Sep 2026 | Helven | Deliver job falls back to the announcement channel when a concept's room exists but is CLOSED | Previously that branch posted nowhere, so a late answer on a closed room was never pinned |
| 18 Sep 2026 | Helven | Heat map buckets `ItemStatus.updatedAt` by Asia/Jakarta calendar day (`jakartaDay` in core), columns built with the same helper | Rule 10: dates stored UTC, displayed in Jakarta; UTC buckets shifted evening reports to the next column |
| 18 Sep 2026 | Helven | `apps/web/next.config.ts` loads the repo-root `.env` via dotenv (`override: false`); `dotenv` declared in the web workspace | `next dev` only reads `apps/web/.env*`, so Auth.js threw `MissingSecret` and Prisma had no `DATABASE_URL` in local runs. The bot already did the same in `config.ts`; Vercel and shell variables win because nothing is overridden |
